//EX 1
const power = (base, exponent) => {
    let result = 1;
    for ( let count = 0; count < exponen; count++) {
        result *= base;
    }
    return result;
};

//EX 2
const square1 = (x) => { return x * x; };
const square2 x => x * x;

//EX 3
const horn = () => {
    console.log("Toot");
};
